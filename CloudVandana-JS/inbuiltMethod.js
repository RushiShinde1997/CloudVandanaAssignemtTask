// Sample array
const elements = [5, 2, 9, 1, 7, 6];

// Sorting the array in descending order
elements.sort((a, b) => b - a);

console.log(elements); // This will also print [9, 6, 5, 5, 2, 1] in descending order
